# Healen OS — frontend
# La anon key es pública por diseño: PostgREST + RLS protegen los datos.
# NUNCA pongas aquí la service role (bypassa RLS; solo backend/servidor).
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon-key>
